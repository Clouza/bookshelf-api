const configuration = [
  {
    'rules': {
      'quotes': ['warn', 'single'],
      'semi': ['error', 'always'],
      'comma-dangle': ['error', 'never'],
      'no-unused-vars': 'warn'
    }
  }
];

export default configuration;